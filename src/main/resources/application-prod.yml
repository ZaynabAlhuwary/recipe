spring:
  datasource:
    url: jdbc:mysql://dbCon:3306/recipe_prod
    username: zozo
    password: zozo
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false
